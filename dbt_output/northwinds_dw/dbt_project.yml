name: 'northwinds_dw'
version: '1.0.0'
config-version: 2

profile: 'northwinds_dw'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  northwinds_dw:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +schema: northwinds_dw
